# Exact target-vision import

The canonical source is not reconstructed from memory or from normalized documents.
The exact Git object was imported from `onedayonemasterpiece/idea-hub` with:

```bash
python scripts/import_source_material.py --source-repo /path/to/idea-hub
```

The command extracts commit `0c3fcf71b2ee8ba8afa49624bef4b779873802f7` and path
`ideas/portfolio.inbox/idea-20260809-content-platform-current-design.md`, writes the
exact bytes to this directory, verifies SHA-256
`c7efb28231223caa6fd02fcc001a38e0f16bcc3fa4c4cd53e744721b2eac0852` and records
the provenance status as `verified_import`.

`my-data-hub` is the canonical project name. `content-platform` is its historical alias,
not a separate project. This verified source import does not prove access to any Region
Talk donor or authorize migration, scheduling or publication.
