name: dataset-loop-mcp-sync-native-evidence-disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  prohibited:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Dataset Loop self-hosted runner workflows are prohibited by owner decision.' >&2
          echo 'Use only an explicitly approved deployment execution path.' >&2
          exit 78
